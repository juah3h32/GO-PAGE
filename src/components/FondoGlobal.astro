---
/* src/components/FondoGlobal.astro */
---

<div class="fondo-global"></div>

<style>
  :root {
    /* 1. MODO CLARO (BLANCO): Sin degradado */
    --bg-gradient: none;
    
    /* Opcional: Si quieres que sea totalmente transparente o blanco explícito */
    /* --bg-gradient: linear-gradient(to right, white, white); */
  }

  :global(.dark) {
    /* 2. MODO OSCURO: Aquí activamos el degradado */
    /* He movido aquí tu degradado principal (el complejo) para que se vea en Dark Mode */
    --bg-gradient: linear-gradient(
      70deg,
      #ff7a18 0%,
      #fb670b 35%,
      #000000 55%,
      #000000 85%
    );
  }

  .fondo-global {
    position: fixed;
    top: 50%;
    left: 50%;
    width: 2000px;
    height: 1200px;
    
    /* Usará 'none' en blanco y el degradado en oscuro */
    background-image: var(--bg-gradient);
    
    border-radius: 20% 30% 80% 10%;
    filter: blur(150px);
    transform: translate(-10%, -50%);
    z-index: -10;
    pointer-events: none;
    
    /* Nota: background-image no se puede animar suavemente (fade). 
       Si quieres un efecto suave al cambiar, es mejor usar opacidad */
    transition: background-image 1s ease, transform 1s ease;
  }
</style>